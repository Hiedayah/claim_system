= render 'submit_to_verifier_form' if allowed_to?(:submit?, @claim) && !current_staff.admin_view?
= render 'verify_return_form' if allowed_to?(:verify?, @claim) && current_staff.admin_view?
= render 'approve_and_return_form' if allowed_to?(:approve?, @claim) && current_staff.admin_view?
